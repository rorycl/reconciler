```yaml
# config.yaml
# Please fill in these details from your Xero OAuth2 App.

client_id: "YOUR_XERO_CLIENT_ID"
client_secret: "YOUR_XERO_CLIENT_SECRET"

# Default paths used by the application.
# The 'wipe' command will target these files.
token_file_path: "./xero_token.json"
database_path: "./reconciliation.db"

# Default financial year start date (YYYY-MM-DD).
# This is used by sync commands if the --fromDate flag is not provided.
financial_year_start: "2025-04-01"
